{
  "name": "cesium-hooks-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -F example dev",
    "build": "pnpm -F cesium-hooks build"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/react": "^18.0.0"
  }
}
